<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="${project.name}">
  <bannerLeft>
    <name>${project.name}</name>
  </bannerLeft>
  <body>
    <links>
      <item name="${project.name}" href="${project.url}"/>
    </links>
    <menu name="References">
      <item name="Javadoc" href="./jdocs"/> 
    </menu>
    ${parentProject}
    ${modules}
    ${reports}
  </body>
</project>
